{
  "buildCommand": "npm ci --include=dev && npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://cms.belims.co.za/wp-json/:path*"
    },
    {
      "source": "/:path*",
      "destination": "/index.html"
    }
  ]
}
